@using VendingAdSystem.Application.DTOs
@model List<KpiViewModel>

<section class="kpi-grid">
@foreach (var kpi in Model)
{
    <article class="kpi-card tone-border-@kpi.Tone">
        <div class="kpi-icon tone-@kpi.Tone"><i class="bi @kpi.Icon"></i></div>
        <div class="kpi-content">
            <p class="kpi-label">@kpi.Label</p>
            <h3 class="kpi-value">@kpi.Value</h3>
            @if (!string.IsNullOrWhiteSpace(kpi.Description))
            {
                <small class="kpi-note">@kpi.Description</small>
            }
        </div>
    </article>
}
</section>
